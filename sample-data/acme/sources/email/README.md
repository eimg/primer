# Acme email fixture

These RFC 5322-style messages are prepared context for a later email source family. They are not part of the first Markdown/Slack retrieval slice.

`metadata.json` supplies fixture project and access information that would normally come from mailbox ownership and folder policy. Message IDs and `In-Reply-To` headers provide thread identity.
