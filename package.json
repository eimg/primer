{
  "name": "@eimg/primer",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Inspect organizational sources as authorized evidence and cited answers",
  "license": "MIT",
  "author": "Ei Maung",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "primer": "./dist/cli.js"
  },
  "files": [
    "dist",
    "sample-data",
    "scripts",
    "README.md",
    "AGENTS.md",
    "docs"
  ],
  "scripts": {
    "build": "npm run build:server && npm run build:web",
    "build:server": "tsc -p tsconfig.build.json",
    "build:web": "vite build",
    "dev": "node --env-file=.env --import tsx src/cli.ts",
    "dev:offline": "PRIMER_DATA_DIR=.primer/offline PRIMER_EMBEDDING_PROVIDER=deterministic PRIMER_CHAT_PROVIDER=deterministic node --import tsx src/cli.ts",
    "dev:readiness:offline": "PRIMER_DATA_DIR=.primer/readiness-offline PRIMER_EMBEDDING_PROVIDER=deterministic PRIMER_CHAT_PROVIDER=deterministic node --import tsx src/cli.ts",
    "dev:readiness:live": "PRIMER_DATA_DIR=.primer/readiness-live node --env-file=.env --import tsx src/cli.ts",
    "dev:api": "node --env-file=.env --import tsx src/server.ts",
    "dev:api:offline": "PRIMER_DATA_DIR=.primer/web-offline PRIMER_EMBEDDING_PROVIDER=deterministic PRIMER_CHAT_PROVIDER=deterministic node --import tsx src/server.ts",
    "dev:web": "vite",
    "dev:full": "concurrently -k -n api,web -c green,cyan \"npm run dev:api\" \"npm run dev:web\"",
    "dev:full:offline": "concurrently -k -n api,web -c green,cyan \"npm run dev:api:offline\" \"npm run dev:web\"",
    "start": "node --env-file=.env dist/server.js",
    "baseline:offline": "npm run dev:offline -- init && npm run dev:offline -- sources ingest && npm run dev:offline -- evaluate",
    "baseline:live": "npm run dev -- init && npm run dev -- sources ingest && npm run dev -- evaluate",
    "baseline:answers:offline": "npm run dev:offline -- init && npm run dev:offline -- sources ingest && npm run dev:offline -- evaluate answers",
    "baseline:answers:live": "npm run dev -- init && npm run dev -- sources ingest && npm run dev -- evaluate answers",
    "readiness:offline": "npm run dev:readiness:offline -- init && npm run dev:readiness:offline -- sources register sample-data/acme/sources/markdown --connector markdown-local && npm run dev:readiness:offline -- sources register sample-data/acme/sources/slack --connector slack-export && npm run dev:readiness:offline -- sources sync && npm run dev:readiness:offline -- readiness check --include-answers",
    "readiness:live:retrieval": "npm run dev:readiness:live -- init && npm run dev:readiness:live -- sources register sample-data/acme/sources/markdown --connector markdown-local && npm run dev:readiness:live -- sources register sample-data/acme/sources/slack --connector slack-export && npm run dev:readiness:live -- sources sync && npm run dev:readiness:live -- readiness check",
    "readiness:live": "npm run dev:readiness:live -- init && npm run dev:readiness:live -- sources register sample-data/acme/sources/markdown --connector markdown-local && npm run dev:readiness:live -- sources register sample-data/acme/sources/slack --connector slack-export && npm run dev:readiness:live -- sources sync && npm run dev:readiness:live -- readiness check --include-answers",
    "test": "node --test --import tsx test/*.test.ts",
    "typecheck": "tsc --noEmit && tsc -p web/tsconfig.json",
    "verify": "npm run typecheck && npm test && npm run build && npm audit"
  },
  "dependencies": {
    "@openrouter/ai-sdk-provider": "^3.0.0",
    "@openrouter/sdk": "^0.13.66",
    "ai": "^7.0.31",
    "better-sqlite3": "^12.11.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20.19.33",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "concurrently": "^9.2.4",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vite": "^8.1.5"
  }
}
